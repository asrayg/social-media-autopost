# =============================================================================
# Database
# =============================================================================
# PostgreSQL connection string used by Prisma.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:password@localhost:5432/social_autopost?schema=public"

# =============================================================================
# Redis
# =============================================================================
# Redis connection string used by BullMQ and ioredis.
# Format: redis://[:password@]HOST:PORT[/db-number]
REDIS_URL="redis://localhost:6379"

# =============================================================================
# NextAuth
# =============================================================================
# A long, random, secret string used to sign/encrypt session tokens.
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="replace-with-a-very-long-random-string"

# The canonical URL of your deployment — no trailing slash.
# In development this is typically http://localhost:3000
NEXTAUTH_URL="http://localhost:3000"

# =============================================================================
# File Storage Directories
# =============================================================================
# Absolute path where user-uploaded raw media files are stored.
UPLOAD_DIR="/Users/you/social-media-autopost/uploads"

# Absolute path where Playwright browser session data (cookies, storage) is
# persisted so accounts stay logged in between runs.
SESSIONS_DIR="/Users/you/social-media-autopost/sessions"

# Absolute path where worker log files are written.
LOGS_DIR="/Users/you/social-media-autopost/logs"

# Absolute path where post-processed (resized / re-encoded) media files are
# stored before being published.
PROCESSED_DIR="/Users/you/social-media-autopost/processed"

# =============================================================================
# Bluesky (AT Protocol API — no browser session needed)
# =============================================================================
# Your Bluesky handle (e.g. name.bsky.social). Defaults to the account username.
BLUESKY_IDENTIFIER=
# An App Password from Bluesky → Settings → App Passwords (NOT your main password).
BLUESKY_APP_PASSWORD=
# Optional custom PDS service (defaults to https://bsky.social).
BLUESKY_SERVICE=


# =============================================================================
# Notifications
# =============================================================================
# Optional webhook to alert you when an account gets logged out (needs re-login).
# Works with Discord/Slack incoming webhooks or any JSON endpoint. Leave blank to
# rely only on the in-app banner.
NOTIFY_WEBHOOK_URL=

# =============================================================================
# TikTok photo carousels (native, swipeable multi-image posts)
# =============================================================================
# TikTok's web uploader is video-only, so real photo carousels are posted one of
# two ways:
#   android (default) — drive TikTok Lite on a running, logged-in Android
#                        emulator via adb/uiautomator (no app audit needed).
#   api               — the official Content Posting API (needs an approved app).
TIKTOK_CAROUSEL_MODE=android
# Instagram Stories are also emulator-only (the web has no story creation) and
# use the same emulator. See docs/EMULATOR-SETUP.md for the one-time setup.
# adb is auto-resolved from ANDROID_HOME / the default SDK path (incl. adb.exe on
# Windows). Override only if your emulator differs from a 1080x2400 Pixel-7 AVD:
#   TT_ANDROID_SERIAL=emulator-5554   # default emulator (per-account: set androidSerial)
#   TT_ANDROID_PKG=com.tiktok.lite.go
#   IG_ANDROID_PKG=com.instagram.android
# Screen-tap coordinates for the native editor/post pages (x,y):
#   TT_ANDROID_PLUS=540,2273
#   TT_ANDROID_EDITOR_NEXT=792,2174
#   TT_ANDROID_CAPTION=450,608
#   TT_ANDROID_POST=794,2232
